<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Upload Audio File</title>
</head>
<body>
    <h1>Upload an Audio File</h1>
    <form action="/upload" method="post" enctype="multipart/form-data">
        <input type="file" name="audioFile" accept="audio/*" required>
        <button type="submit">Upload</button>
    </form>


</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Upload Audio File</title>
</head>
<style>
    body{
        background-color: #eedbbb;
        position: fixed;
        font-family: sans-serif;
        color: #2c3e50;
        margin: 0;
        width: 100%;
        height: 100%;
        cursor: default;
        user-select: none;
    }
    .upload-container{
        color: #cB6843;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100%;
        width: 100%;
        position: fixed;
        left: 0;
        right: -10%;
        bottom: 20%;
        width: 400px;
        height: 33%;
        
        margin: 0 auto 5vh auto;
    }
    .upload-button {
    color: rgb(223, 231, 146);
    display: flex;
    align-items: center;
    justify-content: center; 
    position: fixed;
    left: 50%; 
    bottom: 10%; 
    transform: translateX(-50%); 
    width: 150px; 
    height: 50px; 
    margin: 0 auto;
    border: 2px solid rgb(223, 231, 146); 
    background-color: #cB6843; 
    border-radius: 5px; 
    cursor: pointer; 
    text-decoration: none; 
    font-size: 16px; 
}

.drop-container:hover {
  background:rgb(227, 235, 163);
  border-color: #111;
}
.drop-container:hover .drop-title {
  color: #222;
}
.drop-title {
  color: #444;
  font-size: 20px;
  font-weight: bold;
  text-align: center;
  transition: color .2s ease-in-out;
}
.drop-container {
  position: relative;
  display: flex;
  gap: 10px;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: 200px;
  left: -40%;
  padding: 20px;
  border-radius: 10px;
  border: 2px dashed #555;
  bottom: 50%;
  color: #444;
  cursor: pointer;
  /* transition: background .2s ease-in-out, border .2s ease-in-out; */
}

.upload-nav {
    background-color: #cB6843;
    height: 80px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 1.2rem;
    position: sticky;
    border-bottom: solid;
    border-color: #203354;
}

.upload-nav-container{
    display: flex;
    justify-content:center;
    align-items: center;
    height: 100%;
    width: 100%;
    max-width: 1100px;
    margin: none;
    padding: 0 50px;
}
</style>
<body>
    <nav class = "upload-nav">
        <div class = "upload-nav-container">
            <h1>Upload your songs here to find the chords and their Genre</h1>
        </div>
    <div class = "upload-container">
    <form action="/upload" method="post" enctype="multipart/form-data">
        <label for = "audio" class = "drop-container" id = "dropcontainer">
        <input type="file" class= "choose-button"name="audioFile" accept="audio/*" required class="file-input">
        </label>
        <button type="submit" class="upload-button">Upload</button>
    </form>
    </div>
</body>
</html>