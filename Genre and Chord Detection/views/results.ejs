<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Results</title>
    <link rel="stylesheet" href="<%= bootstrapCSS %>">
    <style>
        body { background-color: #eedbbb; }
        .card {
            height: 220px; width: 220px;
            background-color: rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            margin: auto;
        }
        .container h2 {
            text-align: center;
            margin-top: 20px; margin-bottom: 40px;
            color: #333;
        }
        .card-title { color: #333; font-weight: bold; font-size: 1.2rem; }
        .card-text { color: #333; font-size: 1.5rem; }
        .chord-card { background-color: rgba(251, 190, 9, 0.4); }
        .genre-card { background-color: rgba(23, 162, 184, 0.4); }
        .centered-container {
            display: flex; justify-content: center; text-align: center; 
        }
        .centered-container h1 { margin: 0; }
    </style>
</head>
<body>
<div class="centered-container">
    <h1>Results</h1>
</div>

<div class="container mt-5">
    <!-- Chords Row -->
    <div class="row justify-content-center mb-5">
        <h2 class="w-100 text-center">Chords</h2>
        <% chords.forEach((chord, index) => { %>
        <div class="col mb-4 d-flex align-items-stretch">
            <div class="card chord-card">
                <div class="card-body d-flex flex-column justify-content-center align-items-center">
                    <h5 class="card-title"><%= chord.label %></h5>
                    <p class="card-text"><%= (chord.score * 100).toFixed(2) %> %</p>
                </div>
            </div>
        </div>
        <% }) %>
    </div>

    <!-- Genres Row -->
    <div class="row justify-content-center">
        <h2 class="w-100 text-center">Genres</h2>
        <% genres.forEach((genre, index) => { %>
        <div class="col mb-4 d-flex align-items-stretch">
            <div class="card genre-card">
                <div class="card-body d-flex flex-column justify-content-center align-items-center">
                    <h5 class="card-title"><%= genre.label.charAt(0).toUpperCase() + genre.label.slice(1) %></h5>
                    <p class="card-text"><%= (genre.score * 100).toFixed(2) %> %</p>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
